#ifndef OS_HANDLE_H
#define OS_HANDLE_H

#include <os/types.h>

/* os_handle_t is defined by <os/types.h>. */
/* Public handle contract: opaque authority-bearing reference to a kernel object. */
#define OS_HANDLE_INVALID ((os_handle_t)0u)

#endif /* OS_HANDLE_H */
